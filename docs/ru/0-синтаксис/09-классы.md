# Работа с классами

В этом документе описано, как определять и использовать классы в DataCode: объявление, поля, конструкторы, методы, наследование и видимость (private, protected, public).

**Примеры использования:**
- Классы, конструкторы, поля, методы (EN): [`examples/en/01-basics/classes.dc`](../../../examples/en/01-basics/classes.dc)
- Наследование, super, видимость (EN): [`examples/en/01-basics/inheritance.dc`](../../../examples/en/01-basics/inheritance.dc)
- Классы, конструкторы, поля, методы (RU): [`examples/ru/01-основы/классы.dc`](../../../examples/ru/01-основы/классы.dc)
- Наследование, super, видимость (RU): [`examples/ru/01-основы/наследование.dc`](../../../examples/ru/01-основы/наследование.dc)
- Специальные методы `@add`, `@string`, … (RU): [`examples/ru/02-синтаксис/методы-операции.dc`](../../../examples/ru/02-синтаксис/методы-операции.dc)

---

## Содержание

1. [Объявление класса](#объявление-класса)
2. [Абстрактные классы](#абстрактные-классы)
3. [Секции видимости](#секции-видимости)
4. [Поля](#поля)
5. [Переменные уровня класса](#переменные-уровня-класса)
6. [Конструктор](#конструктор)
7. [Наследование](#наследование)
8. [Методы](#методы)
9. [Параметр @class](#параметр-class)
10. [Доступ к полям](#доступ-к-полям)
11. [Специальные методы (@-methods)](#специальные-методы--methods)

---

## Объявление класса

Класс объявляется ключевым словом `cls`. Тело класса заключается в фигурные скобки.

**Синтаксис без наследования:**

```datacode
cls ClassName {
    # поля, конструкторы, методы
}

```

**Синтаксис с наследованием:**

```datacode
cls ChildClass(ParentClass) {
    # поля, конструкторы, методы
}

```

Имя родительского класса в скобках должно ссылаться на ранее определённый класс.

---

## Абстрактные классы

Класс можно пометить как абстрактный, указав атрибут `@Abstract` непосредственно перед `cls`. Абстрактный класс нельзя инстанцировать напрямую; попытка вызова (например, `AbstractBase()`) приводит к ошибке времени выполнения: `Cannot instantiate abstract class 'AbstractBase'`. Наследники абстрактного класса — обычные классы, их можно инстанцировать. Конструкторы и вызов `super(...)` в наследниках работают как обычно.

**Синтаксис:**

```datacode
@Abstract
cls AbstractBase {
    public:
        name: str
    new AbstractBase(name) {
        this.name = name
    }
}

cls Concrete(AbstractBase) {
    new Concrete(name) {
        super(name)
    }
}

c = Concrete("ok")   # разрешено
# b = AbstractBase("x")  # ошибка времени выполнения: Cannot instantiate abstract class 'AbstractBase'

```

---

## Секции видимости

Члены класса группируются по видимости: `private:`, `protected:` и `public:`.

- **private** — доступ только внутри класса, который определяет член.
- **protected** — доступ в определяющем классе и в наследниках (не снаружи).
- **public** — доступ откуда угодно (поля и методы экземпляра).

Секции необязательны. Поля, методы и переменные уровня класса **без** предшествующей секции по умолчанию **public**. Чтобы скрыть член, явно укажите `private:` или `protected:`. Секции можно повторять (например, снова открыть `public:` после блока `private:`).

```datacode
cls Example {
    name: str              # public (по умолчанию)

    private:
    secret: int

    protected:
    internal: str

    new Example(name) {
        this.name = name
    }
}

```

---

## Поля

Поля объявляются с опциональным типом и опциональным значением по умолчанию.

**Формы:**

- `name: type` — поле с типом, без значения по умолчанию
- `name: type = expression` — поле с типом и значением по умолчанию (константные выражения по возможности вычисляются на этапе компиляции)

Поля можно объявлять **без секции** (тогда они public) или в секциях `private:`, `protected:`, `public:`.

```datacode
cls Point {
    x: int
    y: int
    new Point(x, y) {
        this.x = x
        this.y = y
    }
}

```

Явная секция `public:` нужна только если перед этим был блок `private:` / `protected:`:

```datacode
cls Counter {
    private:
        n: int = 0
    public:
        name: str
    new Counter(name) {
        this.name = name
    }
}

```

---

## Переменные уровня класса

Переменные уровня класса — это присваивания без аннотации типа: `name = expression`. Они хранятся в объекте класса (например, для конфигурации). Подчиняются тем же секциям видимости, что и поля.

```datacode
cls Config {
    public:
        model_config = Settings.config(env_prefix="APP__")
}

```

---

## Конструктор

Конструктор объявляется как `new ClassName(params)` с телом в фигурных скобках.

**Синтаксис:**

```datacode
new ClassName(param1, param2, ...) {
    # тело: присваивание this.field, вызов super(...) при наследовании и т.д.
}

```

**Создание экземпляра:** вызовите класс как функцию с тем же числом аргументов. Среда выбирает конструктор по количеству аргументов (например, `ClassName::new_2` для двух параметров).

Если у всех параметров конструктора указаны типы (`new Foo(x: int)`), внутреннее имя конструктора включает суффикс типов (`Foo::new_1_int`). При вызове `Foo(42)` или `Foo(x)` из другого файла (`from module import Foo`) компилятор и импорт сопоставляют typed-имя автоматически — писать `Foo::new_1_int` в коде не нужно.

```datacode
p = Point(10, 20)
c = Counter("MyCounter")

```

**Делегирующий конструктор:** чтобы вызвать другую перегрузку конструктора того же класса, после списка параметров укажите `: this(...)`. Тело может быть пустым или содержать дополнительную инициализацию, которая выполняется после возврата из делегируемого конструктора:

```datacode
new ClassName(a, b) : this(a + b) { }

new FenwickTree(arr: array[int]) : this(len(arr)) {
    for i in range(len(arr)) {
        this.add(i, arr[i])
    }
}
```

При typed-перегрузках конструкторов (`new Foo(n: int)` и `new Foo(arr: array[int])`) `: this(...)` выбирает подходящую перегрузку по типам аргументов (например, `len(arr)` делегирует конструктору с параметром `int`).

**Примечание:** для побитовых операций над целыми (например, lowbit в Fenwick tree: `i & (-i)`) используйте оператор `&`, а не логический `and`.

---

## Наследование

Чтобы унаследовать класс, объявите `cls Child(Parent) { ... }`.

**Конструктор родителя:** в конструкторе класса-наследника нужно ровно один раз вызвать конструктор родителя первой инструкцией: `super(args)`.

```datacode
cls Child(Parent) {
    public:
    new Child(v) {
        super(v, 202)
    }
}

```

**Вызов метода родителя:** внутри метода используйте `super.methodName(args)`. Это допустимо только внутри класса с родителем.

```datacode
fn overridden() {
    return super.overridden() + 1
}

```

Приватные члены родителя недоступны в наследнике. Защищённые (protected) доступны в классе-наследнике (например, в методах); среда выполнения запрещает чтение и запись protected-полей снаружи иерархии классов.

---

## Аннотации типов параметров

В сигнатуре функции или метода можно указать имя класса как тип параметра: `fn merge(a: HashSet, b: HashSet) { ... }`. Во время выполнения VM проверяет аргумент по метаданным экземпляра:

- **`object` / `dict`** — любой объект-словарь, в том числе экземпляр пользовательского класса.
- **Имя класса** (`HashSet`, `MyClass`, …) — значение должно быть `Value::Object` с `__class_name`, совпадающим с именем, либо с `__superclass`, равным этому имени (прямой родитель). Имена чувствительны к регистру.
- Встроенные типы (`int`, `str`, `array`, …) — отдельный фиксированный набор языка; они не пересекаются с пользовательскими классами по имени.

`typeof(instance)` для экземпляра класса возвращает имя класса (например `"HashSet"`), а не `"object"`. `isinstance(x, "MyClass")` и `isinstance(x, MyClass)` используют ту же логику сопоставления.

**Ограничение:** проверка по цепочке предков глубже одного уровня (`GrandChild` → параметр `Base`, если у экземпляра только `__superclass = "Child"`) в текущей версии не выполняется — планируется обход иерархии через объекты классов в globals.

---

## Методы

Методы объявляются как `fn methodName(params) { body }`. Опционально можно указать тип возвращаемого значения: `fn methodName(params) -> type { body }`. Первый неявный параметр — `this` (текущий экземпляр).

```datacode
cls Calculator {
    public:
        base: int
    new Calculator(base) {
        this.base = base
    }
    fn add(n) {
        return this.base + n
    }
}

```

**Вызов метода:** `instance.methodName(args)`.

```datacode
calc = Calculator(100)
print(calc.add(25))  # 125

```

---

## Параметр @class

Метод может объявить необязательный первый параметр `@class`, чтобы получить объект класса (мета-информацию о классе). Он должен быть первым параметром в списке. VM автоматически подставляет объект класса при вызове метода; вызывающий код его не передаёт.

**Синтаксис:** `fn methodName(@class, otherParams) { body }`

**Пример:**

```datacode
cls User {
    public:
        name: str
    new User(name) {
        this.name = name
    }
    fn info(@class) {
        print(@class.name)           # "User"
        print(@class.full_name)      # "User"
        print(@class.method_names)   # ["info", ...]
    }
}

u = User("Alice")
u.info()   # @class подставляется; выводит имя класса и список методов

```

**API объекта @class (свойства объекта класса):**

- **Идентификация:** `@class.name` (короткое имя), `@class.full_name` (пока совпадает с name)
- **Иерархия:** `@class.parent` (имя суперкласса или null)
- **Модификаторы:** `@class.is_abstract` (true, если класс абстрактный)
- **Структура:** `@class.method_names` (массив имён методов)

Используйте `@class` для регистрации в ORM, сериализации, DI или любой логики, которой нужно знать класс во время выполнения без изменения мест вызова.

---

## Доступ к полям

- **Внутри класса:** используйте `this.fieldName` для чтения и присваивания полей экземпляра в конструкторах и методах.
- **Снаружи:** используйте `object.fieldName` для публичных полей. Обращение к private или protected полям снаружи класса (или из «неправильного» класса в иерархии) приводит к ошибке времени выполнения (например, ProtectError).

```datacode
# В конструкторе или методе
this.x = 10
v = this.x

# Снаружи (только public)
p = Point(5, 10)
print(p.x)
print(p.y)

```

---

## Специальные методы (@-methods)

Методы вида `fn @add(...)`, `fn @string()` и т.д. подключают экземпляр класса к операторам (`+`, `==`, `in`, …), встроенным функциям (`str`, `len`, `print`) и протоколу `for-in`. Вызывать их напрямую (`obj.@string()`) нельзя.

Подробная таблица методов, сигнатуры и пример `Vector`: **[Специальные методы](../2-язык/специальные-методы.md)**.

---

## См. также

**Примеры использования:**
- Классы, конструкторы, поля, методы (EN): [`examples/en/01-basics/classes.dc`](../../../examples/en/01-basics/classes.dc)
- Наследование, super, видимость (EN): [`examples/en/01-basics/inheritance.dc`](../../../examples/en/01-basics/inheritance.dc)
- Классы, конструкторы, поля, методы (RU): [`examples/ru/01-основы/классы.dc`](../../../examples/ru/01-основы/классы.dc)
- Наследование, super, видимость (RU): [`examples/ru/01-основы/наследование.dc`](../../../examples/ru/01-основы/наследование.dc)
- Специальные методы `@add`, `@string`, … (RU): [`examples/ru/02-синтаксис/методы-операции.dc`](../../../examples/ru/02-синтаксис/методы-операции.dc)
