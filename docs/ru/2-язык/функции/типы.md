# Работа с типами

← [Встроенные функции](./README.md)

Подробнее о проверке типов в контексте языка: [typeof и isinstance](../типы-данных/typeof-и-isinstance.md).

**📚 Примеры:** [`examples/ru/03-типы данных/`](../../../examples/ru/03-типы%20данных/)

### `typeof(value)`

Возвращает строку с типом значения.

**Аргументы:**
- `value` (any) - значение для определения типа

**Возвращает:** `string` - название типа ("int", "float", "string", "bool", "array", "table", "path", "null", "function", "date", "money", "object")

**Примеры:**
```datacode
typeof(42)              # "int"
typeof(3.14)            # "float"
typeof("hello")         # "string"
typeof(true)            # "bool"
typeof([1, 2, 3])       # "array"
typeof(null)            # "null"
typeof(path("test.txt")) # "path"

```

---

### `isinstance(value, type_name)`

Проверяет, является ли значение экземпляром указанного типа.

**Аргументы:**
- `value` (any) - значение для проверки
- `type_name` (string) - название типа для проверки

**Возвращает:** `bool` - `true` если значение соответствует типу, иначе `false`

**Примеры:**
```datacode
isinstance(42, "int")           # true
isinstance(3.14, "float")       # true
isinstance("hello", "string")   # true
isinstance([1, 2], "array")     # true
isinstance(42, "string")        # false

```

---
