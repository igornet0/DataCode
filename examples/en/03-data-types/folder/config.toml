# Sample test config.toml

name = "Example configuration"
version = 1
debug = true

[paths]
input = "input/"
output = "output/"

[database]
host = "localhost"
port = 5432
user = "admin"
password = "secret"