from base import DevAppBaseConfig
from config import ConfigApp, DatabaseConfig

# Configuration for development environment
cls DevSettings(ConfigApp) {
    model_config = DevAppBaseConfig
    env = "dev"
    debug = true
    db = DatabaseConfig()
}
