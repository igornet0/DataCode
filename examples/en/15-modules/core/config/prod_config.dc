from base import ProdAppBaseConfig
from config import ConfigApp, DatabaseConfig

# Configuration for production environment
cls ProdSettings(ConfigApp) {
    model_config = ProdAppBaseConfig
    env = "prod"
    debug = false
    db = DatabaseConfig()
}
