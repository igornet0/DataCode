# 📦 Типы данных в DataCode

Этот раздел демонстрирует работу с различными типами данных в DataCode, включая преобразование типов, проверку типов и работу с объектами.

## 📋 Содержание

### 1. `функции_преобразования_типов.dc` - Функции преобразования типов
**Описание**: Демонстрирует все функции преобразования типов, доступные в DataCode.

**Что изучается**:
- Преобразование между типами с помощью `int()`, `float()`, `bool()`, `str()`
- Создание массивов с помощью `array()`
- Работа с датами с помощью `date()`
- Форматирование денег с помощью `money()`
- Определение типов с помощью `typeof()`
- Практические примеры преобразования типов

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/функции_преобразования_типов.dc
```

### 2. `руководство_преобразования_типов.dc` - Руководство по преобразованию типов
**Описание**: Подробное руководство по работе с типами и преобразованиям.

**Что изучается**:
- Базовые типы данных
- Проверка типов с помощью `isinstance()`
- Стратегии преобразования типов
- Работа с различными комбинациями типов

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/руководство_преобразования_типов.dc
```

### 3. `тип_дата.dc` - Работа с датами
**Описание**: Демонстрирует работу с датами и форматированием.

**Что изучается**:
- Создание дат
- Форматы дат
- Работа со строковыми датами

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/тип_дата.dc
```

### 4. `объекты.dc` - Работа с объектами
**Описание**: Демонстрирует создание и работу с объектами (словарями) в DataCode.

**Что изучается**:
- Создание объектов с парами ключ-значение
- Доступ к свойствам через точку (`obj.property`)
- Доступ к свойствам через квадратные скобки (`obj['key']`)
- Вложенные объекты
- Объекты с разными типами значений
- Объекты в массивах
- Использование объектов как словарей
- Практические примеры конфигурации

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/объекты.dc
```

### 5. `массивы.dc` — массивы (глобалы и методы)
**Описание**: Создание массивов, `push`/`pop`, `unique`, `reverse`, `sort`, числовые агрегаты, `any`/`all`, и `arr.chunk(n)` (у `chunk` нет глобальной функции).

**Что изучается**:
- `array()` и литералы `[...]`
- `array_with_capacity()` с повторяющимся `push`
- Вызов как функции и как метода (`push(arr, x)` и `arr.push(x)`)
- `unique`, `reverse`, `sort`
- `sum`, `average`, `count` и `len`
- `min` и `max` с несколькими числами или одним массивом чисел (`min([])` даёт `null`)
- Проверка истинности через `any` и `all` (включая пустой массив)
- `map`, `filter` и `reduce` (у `reduce` обязательно начальное значение)
- Разбиение через `arr.chunk(n)`

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/массивы.dc
```

### 7. `таблицы/` — трансформации таблиц (виджет «Трансформация»)
**Описание**: Примеры всех операций трансформации колонок и строк с CSV-данными.

**Что изучается**: `table_select`, `table_sort`, `table_drop_nulls`, текстовые/числовые функции по колонкам, обходные пути для ещё не реализованных `table_*` операций.

**Запуск**:
```bash
datacode "examples/ru/03-типы данных/таблицы/01-колонки.dc"
```

См. [`таблицы/README.md`](таблицы/README.md).

### 6. `table_push.dc` — метод `table.push()`
**Описание**: Добавление строк в таблицу из объекта, массива объектов, массива значений или другой таблицы.

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/table_push.dc
```

### 8. `table_save.dc` — экспорт таблицы
**Описание**: `table.save_csv()` и `table.save_sqlite()`.

**Запуск**:
```bash
datacode examples/ru/03-типы\ данных/table_save.dc
```

## 🎯 Изучаемые концепции
- **int()**: Преобразование в целое число
- **float()**: Преобразование в число с плавающей точкой
- **bool()**: Преобразование в булево значение
- **str()**: Преобразование в строку
- **array()**: Создание массива из аргументов
- **date()**: Создание даты из строки
- **money()**: Форматирование числа как валюты

### Проверка типов
- **typeof()**: Получение имени типа в виде строки
- **isinstance()**: Проверка, является ли значение экземпляром типа

### Массивы
- **Создание**: `array(...)`, `[...]`, `array_with_capacity(n)`
- **Изменение**: `push`, `pop`, `reverse`, `sort`; также форма `arr.method(...)`
- **Копии / представления**: `unique`; разбиение через `arr.chunk(n)` (только метод)
- **Агрегаты**: `sum`, `average`, `count`, `len`; `min`/`max` с несколькими числами или одним числовым массивом; логика: `any`, `all`; `map`, `filter`, `reduce(..., initial)`

### Объекты (Словари)
- **Создание**: `{key: value, key2: value2}`
- **Доступ**: `obj.key` или `obj['key']`
- **Вложенные**: Объекты могут содержать другие объекты
- **Смешанные типы**: Значения могут быть любого типа DataCode
- **Строковые ключи**: Используйте кавычки для ключей с пробелами: `{'key name': value}`

## 🔗 Навигация

### Предыдущие шаги
- **[02-синтаксис](../02-синтаксис/)** - Синтаксические конструкции языка

### Следующие шаги
- **[09-продвинутые](../09-продвинутые/)** - Продвинутые возможности, включая обработку ошибок
- **[04-функции](../04-функции/)** - Создание и использование функций

---

**Продолжайте изучение!** 🚀

