# 🎨 Графики и визуализация (модуль plot)

Этот раздел содержит примеры работы с модулем `plot` для загрузки и отображения изображений, а также построения различных типов графиков в ДатаКод.

## 📋 Содержание

### 1. `01-загрузка-изображения.dc` - Загрузка изображения
**Описание**: Демонстрирует базовую загрузку изображения из файла.

**Что изучается**:
- Функция `plot.image(path)` для загрузки изображений
- Проверка успешности загрузки
- Поддерживаемые форматы изображений

**Запуск**:
```bash
cargo run examples/ru/10-графики/01-загрузка-изображения.dc
```

### 2. `02-показ-изображения.dc` - Быстрый просмотр изображения
**Описание**: Демонстрирует использование удобной функции `plot.show()` для автоматического показа изображения.

**Что изучается**:
- Функция `plot.show(path)` для быстрого просмотра
- Автоматическое создание окна с размерами изображения
- Блокирующее ожидание закрытия окна

**Запуск**:
```bash
cargo run examples/ru/10-графики/02-показ-изображения.dc
```

### 3. `03-создание-окна.dc` - Создание окна
**Описание**: Демонстрирует создание окна с заданными параметрами.

**Что изучается**:
- Функция `plot.window(width, height, title)` для создания окна
- Параметры окна (ширина, высота, заголовок)
- Проверка успешности создания окна

**Запуск**:
```bash
cargo run examples/ru/10-графики/03-создание-окна.dc
```

### 4. `04-рисование-изображения.dc` - Рисование в окне
**Описание**: Демонстрирует полный цикл работы с изображениями: загрузка, создание окна, отрисовка.

**Что изучается**:
- Последовательность операций для отображения изображения
- Функция `plot.draw(window, image)` для отрисовки
- Функция `plot.wait(window)` для ожидания закрытия окна

**Запуск**:
```bash
cargo run examples/ru/10-графики/04-рисование-изображения.dc
```

### 5. `05-полный-пример.dc` - Комплексный пример
**Описание**: Полный пример, демонстрирующий все возможности модуля `plot`.

**Что изучается**:
- Все функции модуля `plot` в одном примере
- Комбинирование различных операций
- Обработка ошибок при работе с изображениями

**Запуск**:
```bash
cargo run examples/ru/10-графики/05-полный-пример.dc
```

### 6. `06-subplots.dc` - Создание сетки subplots
**Описание**: Демонстрирует использование `plot.subplots()` для создания сетки графиков и отображения нескольких изображений одновременно.

**Что изучается**:
- Функция `plot.subplots(rows, cols, figsize=(width, height))` для создания сетки
- Доступ к осям через `fig.axes[row][col]`
- Метод `axis.imshow(image, cmap='gray')` для отображения изображений в осях
- Метод `axis.set_title(title)` для установки заголовков
- Метод `axis.axis('off')` для скрытия координатных осей
- Функция `plot.tight_layout(fig)` для автоматической настройки отступов
- Отображение фигуры через `plot.show(fig, title="...")`

**Запуск**:
```bash
cargo run examples/ru/10-графики/06-subplots.dc
```

### 7. `07-столбчатая-диаграмма.dc` - Столбчатая диаграмма
**Описание**: Демонстрирует построение столбчатой диаграммы (bar chart) с использованием категориальных данных.

**Что изучается**:
- Функция `plot.bar(x, y)` для создания столбчатой диаграммы
- Функции `plot.xlabel()` и `plot.ylabel()` для установки подписей осей
- Работа с категориальными данными (строки) и числовыми значениями
- Функция `plot.show(title="...")` для отображения графика с заголовком

**Запуск**:
```bash
cargo run examples/ru/10-графики/07-столбчатая-диаграмма.dc
```

### 8. `08-тепловая-карта.dc` - Тепловая карта
**Описание**: Демонстрирует построение тепловой карты (heatmap) для визуализации матричных данных.

**Что изучается**:
- Функция `plot.heatmap(data)` для создания тепловой карты
- Параметры `min` и `max` для настройки диапазона значений
- Параметр `palette` для выбора цветовой схемы
- Визуализация двумерных массивов данных

**Запуск**:
```bash
cargo run examples/ru/10-графики/08-тепловая-карта.dc
```

### 9. `09-линейный-график.dc` - Линейный график
**Описание**: Демонстрирует построение линейных графиков с несколькими сериями данных.

**Что изучается**:
- Функция `plot.line(x, y, connected, color)` для создания линейного графика
- Параметр `connected` для соединения точек линией
- Параметр `color` для задания цвета линии (именованные цвета и hex-коды)
- Построение нескольких графиков на одной оси
- Поддерживаемые цвета: blue, green, red, black, white, а также hex-коды

**Запуск**:
```bash
cargo run examples/ru/10-графики/09-линейный-график.dc
```

### 10. `10-круговая-диаграмма.dc` - Круговая диаграмма
**Описание**: Демонстрирует построение круговой диаграммы (pie chart) для визуализации пропорций.

**Что изучается**:
- Функция `plot.pie(x, y)` для создания круговой диаграммы
- Работа с категориальными метками и числовыми значениями
- Визуализация пропорций и долей данных

**Запуск**:
```bash
cargo run examples/ru/10-графики/10-круговая-диаграмма.dc
```

## 🎯 Изучаемые концепции

### Модуль plot
Модуль `plot` предоставляет функциональность для работы с изображениями, их отображения в окнах, а также построения различных типов графиков и диаграмм. Модуль доступен как глобальная переменная `plot`.

### Функции модуля plot

#### `plot.image(path) -> Image | null`
Загружает изображение из файла по указанному пути.

**Параметры**:
- `path` (строка): путь к файлу изображения

**Возвращает**:
- `Image`: объект изображения при успешной загрузке
- `null`: если загрузка не удалась

**Пример**:
```datacode
let img = plot.image("путь/к/изображению.png")
if img != null {
    print("Изображение загружено!")
}
```

#### `plot.window(width, height, title) -> Window | null`
Создает новое окно для отображения графики.

**Параметры**:
- `width` (число): ширина окна в пикселях
- `height` (число): высота окна в пикселях
- `title` (строка): заголовок окна

**Возвращает**:
- `Window`: объект окна при успешном создании
- `null`: если создание не удалось

**Пример**:
```datacode
let окно = plot.window(800, 600, "Мое окно")
```

#### `plot.draw(window, image) -> null`
Рисует изображение в указанном окне.

**Параметры**:
- `window` (Window): объект окна
- `image` (Image): объект изображения

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
plot.draw(окно, изображение)
```

#### `plot.wait(window) -> null`
Блокирует выполнение программы до закрытия окна пользователем.

**Параметры**:
- `window` (Window): объект окна

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
plot.wait(окно)
print("Окно закрыто")
```

#### `plot.show(path) -> null`
#### `plot.show(figure, title="...") -> null`
Удобная функция для быстрого просмотра изображения или фигуры. Автоматически загружает изображение, создает окно с размерами изображения, отображает его и ждет закрытия. Также может отображать фигуру с subplots.

**Параметры**:
- `path` (строка): путь к файлу изображения
- `figure` (Figure): объект фигуры с subplots
- `title` (строка, опционально): заголовок окна

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
plot.show("путь/к/изображению.png")
# или
fig = plot.subplots(2, 2)
plot.show(fig, title="Моя фигура")
```

#### `plot.subplots(rows, cols, figsize=(width, height)) -> Figure`
Создает фигуру с сеткой subplots для отображения нескольких графиков одновременно.

**Параметры**:
- `rows` (число): количество строк в сетке
- `cols` (число): количество столбцов в сетке
- `figsize` (кортеж, опционально): размер фигуры в дюймах (ширина, высота), по умолчанию (10, 10)

**Возвращает**:
- `Figure`: объект фигуры с двумерным массивом осей `axes[row][col]`

**Пример**:
```datacode
fig = plot.subplots(3, 3, figsize=(10, 10))
axis = fig.axes[0][0]  # Доступ к первой оси
```

#### `plot.tight_layout(figure) -> null`
Автоматически настраивает отступы между subplots для более аккуратного отображения.

**Параметры**:
- `figure` (Figure): объект фигуры

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
fig = plot.subplots(2, 2)
# ... настройка subplots ...
plot.tight_layout(fig)
plot.show(fig)
```

#### `plot.bar(x, y) -> null`
Создает столбчатую диаграмму.

**Параметры**:
- `x` (массив строк): категории для оси X
- `y` (массив чисел): значения для оси Y

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
x = ["Категория 1", "Категория 2", "Категория 3"]
y = [10, 20, 15]
plot.bar(x, y)
plot.show(title="Столбчатая диаграмма")
```

#### `plot.line(x, y, connected, color) -> null`
Создает линейный график.

**Параметры**:
- `x` (массив чисел): значения для оси X
- `y` (массив чисел): значения для оси Y
- `connected` (булево): соединять ли точки линией
- `color` (строка, опционально): цвет линии (blue, green, red, black, white или hex-код)

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
x = [1, 2, 3, 4, 5]
y = [2, 4, 1, 6, 3]
plot.line(x, y, true, color="blue")
plot.show(title="Линейный график")
```

#### `plot.pie(x, y) -> null`
Создает круговую диаграмму.

**Параметры**:
- `x` (массив строк): метки для секторов
- `y` (массив чисел): значения для секторов

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
x = ["Элемент 1", "Элемент 2", "Элемент 3"]
y = [30, 40, 30]
plot.pie(x, y)
plot.show(title="Круговая диаграмма")
```

#### `plot.heatmap(data, min, max, palette) -> null`
Создает тепловую карту из матрицы данных.

**Параметры**:
- `data` (массив массивов чисел): двумерный массив данных
- `min` (число, опционально): минимальное значение для цветовой шкалы
- `max` (число, опционально): максимальное значение для цветовой шкалы
- `palette` (строка, опционально): цветовая палитра (например, "red")

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
data = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
plot.heatmap(data)
plot.show(title="Тепловая карта")
```

#### `plot.xlabel(label) -> null`
#### `plot.ylabel(label) -> null`
Устанавливает подпись для оси X или Y.

**Параметры**:
- `label` (строка): текст подписи

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
plot.xlabel("Время")
plot.ylabel("Значение")
```

### Методы объекта Axis

#### `axis.imshow(image, cmap='gray') -> null`
Отображает изображение в оси.

**Параметры**:
- `image` (Image | Tensor): изображение для отображения
- `cmap` (строка, опционально): цветовая карта ('gray', 'viridis' и др.), по умолчанию 'gray'

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
axis = fig.axes[0][0]
axis.imshow(image, cmap='gray')
```

#### `axis.set_title(title) -> null`
Устанавливает заголовок для оси.

**Параметры**:
- `title` (строка): текст заголовка

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
axis.set_title("Мое изображение")
```

#### `axis.axis('off') -> null`
#### `axis.axis('on') -> null`
Управляет видимостью координатных осей.

**Параметры**:
- `mode` (строка): 'off' для скрытия осей, 'on' для показа

**Возвращает**:
- `null`: всегда возвращает null

**Пример**:
```datacode
axis.axis('off')  # Скрыть оси
axis.axis('on')   # Показать оси
```

### Поддерживаемые форматы изображений

Модуль `plot` поддерживает следующие форматы изображений:
- **PNG** (.png)
- **JPEG** (.jpg, .jpeg)
- **GIF** (.gif)
- **BMP** (.bmp)
- И другие форматы, поддерживаемые библиотекой `image` в Rust

### Типы данных

#### Image
Объект изображения, содержащий:
- Данные пикселей в формате RGBA
- Ширину изображения
- Высоту изображения

#### Window
Объект окна, содержащий:
- Размеры окна (ширина, высота)
- Заголовок окна
- Состояние окна (открыто/закрыто)

#### Figure
Объект фигуры с subplots, содержащий:
- Двумерный массив осей `axes[row][col]`
- Размер фигуры `figsize` (ширина, высота)
- Настройки компоновки

#### Axis
Объект оси для отображения графиков, содержащий:
- Изображение для отображения
- Заголовок оси
- Настройки видимости координатных осей
- Цветовую карту (cmap)

## ⚠️ Важные моменты

1. **Пути к файлам**: Используйте правильные пути к файлам изображений. Относительные пути указываются относительно текущей директории выполнения программы.

2. **Блокирующие функции**: Функции `plot.show()` и `plot.wait()` блокируют выполнение программы до закрытия окна пользователем.

3. **Проверка результатов**: Всегда проверяйте результат загрузки изображения и создания окна на `null`, чтобы обработать возможные ошибки.

4. **Центрирование**: Изображение автоматически центрируется в окне, если оно меньше размера окна.

5. **Текущий статус**: Некоторые функции модуля могут быть частично реализованы. Проверяйте актуальную документацию для получения информации о доступных возможностях.

## 🔗 Навигация

### Предыдущие разделы
- **[09-создание модели данных](../09-создание%20модели%20данных/)** - работа с данными и таблицами
- **[08-websocket](../08-websocket/)** - работа с WebSocket
- **[07-циклы](../07-циклы/)** - циклы и итерации

### Следующие шаги
После изучения модуля `plot` вы можете:
- Интегрировать визуализацию в свои проекты
- Создавать инструменты для просмотра изображений
- Разрабатывать приложения с графическим интерфейсом

## 💡 Советы

- **Начните с простого**: Используйте `plot.show()` для быстрого просмотра изображений
- **Обрабатывайте ошибки**: Всегда проверяйте результаты операций на `null`
- **Используйте правильные пути**: Убедитесь, что пути к файлам указаны корректно
- **Экспериментируйте**: Попробуйте различные размеры окон и форматы изображений

## 📚 Дополнительная информация

Для более подробной информации о модуле `plot` и его возможностях обратитесь к основной документации проекта.

---

**Удачи в работе с графикой в ДатаКод!** 🎨✨



