# Примеры DataSource

- `01-http.dc` — HTTP API (нужна сеть)
- `02-file.dc` — чтение CSV/JSON из каталога
- `03-sqlite.dc` — SQLite in-memory
- `04-database/` — PostgreSQL, MySQL, SQLite, MongoDB, MSSQL

Параметры подключения к БД (обязательные и опциональные) — в [`04-database/README.md`](04-database/README.md).
