from core.database.adapters.base import BaseAdapter

cls MariadbAdapter(BaseAdapter) {
    fn connect(config) {
        # mariadb connection logic (same as MySQL)
        raise NotImplementedError()
    }
}
